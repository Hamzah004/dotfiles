#!/bin/bash

# Target date
target_date="2025-12-3"

# Calculate the difference in days
days_left=$((($(date -d "$target_date" +%s) - $(date +%s)) / (60 * 60 * 24)))

# Display the countdown
echo "push_swap deadline: $days_left days"
